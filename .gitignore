node_modules/
__pycache__/
*.pyc
.DS_Store

# Local editor / per-machine config
.claude/settings.local.json

# Downloaded source caches (regenerable from scripts/fetch_modern_state_outlines.py)
data_raw/_cache/

# Generated outputs — fully regenerated by CI (python scripts/build_web_assets.py)
data_processed/
web/data_processed/

# Third-party reference PDFs (not redistributable)
*.pdf
source data/

# Detached tiler run artifacts (regenerable; see CLAUDE.md "Iterating on the tiler")
tiler_run.log
tiler_run.err.log
tiler_run_start.txt

# Local manual backup patches
*.patch

# Local Census API key (never commit)
census_API_key.txt
